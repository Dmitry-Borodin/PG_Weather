Local tool to generate weather paragliding forecast for specific locations 

## Usage
Clone the project, run run.sh script

### Requirements
Script runs in self sufficient isolated podman container. You just need to have podman to run it.
